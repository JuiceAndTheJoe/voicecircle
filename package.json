  "name": "voicecircle-monorepo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "npm --prefix backend install && npm --prefix frontend install && npm --prefix frontend run build",
    "start": "npm --prefix backend start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
